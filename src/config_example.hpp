/*
    This file contains the settings used by the Wio Terminal to connect to Wifi and IoTHub.
    Make a copy as `config.hpp` and fill out the settings to build and run the sample on a Wio Terminal
*/
#include <string>

using namespace std;

// WiFi credentials
const char *SSID = "";
const char *PASSWORD = "";

// IoT Hub settings
const char *IOTHUB_DEVICEID = "";
const char *IOTHUB_CONNECTION_STRING = "";
